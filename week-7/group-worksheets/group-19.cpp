/**
 * group members:
 * week 7
 * worksheet -- group 19
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( (23/29) != a <= b >= !b == a < (13%23) && b >= +(5%13) || -b != !-( b || !(23%11) ) || b ) << endl;















    return 0;
}

