/**
 * group members:
 * week 7
 * worksheet -- group 01
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( !b != !(23%11) && b <= (2%29) && !a <= b == -( b > ( (19/3) < (13%11) != a ) >= !c ) ) << endl;















    return 0;
}

