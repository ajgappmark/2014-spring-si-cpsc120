/**
 * group members:
 * week 7
 * worksheet -- group 17
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( !a || !b < (7%3) == ( !(11%23) && !b == b + c > !-a ) + !b - (23%19) > ( a >= !a >= !b ) ) << endl;















    return 0;
}

