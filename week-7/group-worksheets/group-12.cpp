/**
 * group members:
 * week 7
 * worksheet -- group 12
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << !( (5%7) != b != ( -a - !a ) && a < !(!a) < c - !(5%11) == ( (2/13) == ( (11/2) || c ) ) < +b ) << endl;















    return 0;
}

