/**
 * group members:
 * week 7
 * worksheet -- group 09
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( c && !b > !(23/17) >= (5%2) && ( b == (2%19) > (13/19) || -a || a < !b + a ) == ( c || !a ) ) << endl;















    return 0;
}

