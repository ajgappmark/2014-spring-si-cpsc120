/**
 * group members:
 * week 7
 * worksheet -- group 22
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( b || -(5%2) != !(!a) >= (13%19) - b > a < a == b == (3%13) <= a == +a > ( b || (5%2) ) ) << endl;















    return 0;
}

