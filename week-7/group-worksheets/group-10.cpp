/**
 * group members:
 * week 7
 * worksheet -- group 10
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( !a > -( b < a != ( b > (17/23) < a == !(!b) >= +(7/11) ) && b > -a ) && !(5%11) ) << endl;















    return 0;
}

