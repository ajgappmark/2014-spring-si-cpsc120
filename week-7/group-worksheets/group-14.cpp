/**
 * group members:
 * week 7
 * worksheet -- group 14
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << -( b && a * b <= a > -( (11%17) < !b != (3%11) && b < (19/5) >= c ) >= !a && +a + -(23%19) ) << endl;















    return 0;
}

