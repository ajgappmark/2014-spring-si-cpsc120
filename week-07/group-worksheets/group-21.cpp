/**
 * group members:
 * week 7
 * worksheet -- group 21
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << !( -a != +a >= !a == -a != c && b != b + !a == !(!(17/3)) * -(23%2) || b || a == ( b >= !b ) ) << endl;















    return 0;
}

