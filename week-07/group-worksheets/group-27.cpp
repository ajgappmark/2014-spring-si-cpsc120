/**
 * group members:
 * week 7
 * worksheet -- group 27
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( b <= b < !b > -!( (3%23) > !a >= b >= ( -a < (17%2) ) >= b == a ) <= !a <= b > +b && !a ) << endl;















    return 0;
}

