/**
 * group members:
 * week 7
 * worksheet -- group 25
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( -a - b < !(!( a != +( (7/5) && -(29%17) != c ) > a || (7%5) == c <= (2%29) )) ) << endl;















    return 0;
}

