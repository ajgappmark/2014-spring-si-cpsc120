/**
 * group members:
 * week 7
 * worksheet -- group 05
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( c == ( b <= !c > c * (11/11) ) + c != (11%2) || b || b || c && !( !a > !b + (3/5) > b ) < b ) << endl;















    return 0;
}

