/**
 * group members:
 * week 7
 * worksheet -- group 02
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << +( ( (17%13) + -!b >= -a == b == !b != (2%23) ) != b && c && ( -(3/11) - !+b < a ) < a ) << endl;















    return 0;
}

