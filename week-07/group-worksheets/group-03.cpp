/**
 * group members:
 * week 7
 * worksheet -- group 03
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << -( a && c == -c < -b >= c - !c != ( c >= a || -b == b && !(2/23) <= !+(17%2) || (19%11) ) ) << endl;















    return 0;
}

