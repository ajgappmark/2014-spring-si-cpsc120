/**
 * group members:
 * week 7
 * worksheet -- group 11
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( ( b && (2%23) >= a + c ) == b < a * a <= +(3/17) >= b * b || (11%3) >= !a < b ) << endl;















    return 0;
}

