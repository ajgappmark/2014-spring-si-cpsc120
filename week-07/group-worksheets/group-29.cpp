/**
 * group members:
 * week 7
 * worksheet -- group 29
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << +( a - +(3/3) >= !a != ( a <= (13%2) && ( c >= c ) != !a == c != ( a <= -a ) < +(5/17) ) ) << endl;















    return 0;
}

