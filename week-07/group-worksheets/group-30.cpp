/**
 * group members:
 * week 7
 * worksheet -- group 30
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( ( a + a == !b != (7%23) <= b != c ) > b > a <= a >= b != b + -a >= -!a && (17/29) >= b <= a ) << endl;















    return 0;
}

