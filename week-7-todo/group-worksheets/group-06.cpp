/**
 * group members:
 * week 7
 * worksheet -- group 06
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( c || c || b <= a == (23%7) < c < c <= b < -( (17/3) < a * b < b + +(3/11) <= a ) != !b ) << endl;















    return 0;
}

