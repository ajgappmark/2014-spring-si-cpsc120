/**
 * group members:
 * week 7
 * worksheet -- group 23
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( ( a == !a != !a ) || a != b != c == !c <= -b != !(11%23) + a > ( c || -b > (5/17) ) >= b >= b ) << endl;















    return 0;
}

