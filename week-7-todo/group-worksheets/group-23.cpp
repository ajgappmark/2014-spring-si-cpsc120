/**
 * group members:
 * week 7
 * worksheet -- group 23
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( b < -c >= a <= a || b != b - !a && a > b && (23/11) < (17/5) && b > b <= !+(17%3) > b ) << endl;















    return 0;
}

