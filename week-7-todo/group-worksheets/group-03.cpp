/**
 * group members:
 * week 7
 * worksheet -- group 03
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( c < -( b >= +(17/17) >= a && b < a >= -+b ) == b > b == c >= +-c != -b == -(17/5) != !b && b ) << endl;















    return 0;
}

