/**
 * group members:
 * week 7
 * worksheet -- group 13
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << +( b > c * +(17/13) - (11/13) && a * -a >= (7%11) <= !b == a > b > a > c != b <= b >= !(2/7) ) << endl;















    return 0;
}

