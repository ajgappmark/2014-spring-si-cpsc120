/**
 * group members:
 * week 7
 * worksheet -- group 13
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( c < -!c <= ( c || (13%11) || !b && -b == !(19%3) != !a <= a != a > !(5/17) ) <= -b <= b ) << endl;















    return 0;
}

