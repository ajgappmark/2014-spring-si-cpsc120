/**
 * group members:
 * week 7
 * worksheet -- group 13
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( a != b != b * a != -(19%3) || b < (13%5) && b != a || -b <= b < !(19/13) * -a * (17/2) == b ) << endl;















    return 0;
}

