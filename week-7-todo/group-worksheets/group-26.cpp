/**
 * group members:
 * week 7
 * worksheet -- group 26
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << +( c && (17%29) * b != +b == !c || !b > ( c < (29/7) >= a ) != b > b && ( (3/7) > b < !b > b ) ) << endl;















    return 0;
}

