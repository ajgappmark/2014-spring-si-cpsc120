/**
 * group members:
 * week 7
 * worksheet -- group 26
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << !( (5/3) + (3%7) + !b * !(!a) < a != c <= b < -b != (23/19) < b != b < ( c || -!c ) == a <= a ) << endl;















    return 0;
}

