/**
 * group members:
 * week 7
 * worksheet -- group 22
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( b != ( (29%2) + (19/13) >= b && c < !a ) - a <= b >= b == a || b == !(13%19) != b <= b <= a ) << endl;















    return 0;
}

