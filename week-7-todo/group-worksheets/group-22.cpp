/**
 * group members:
 * week 7
 * worksheet -- group 22
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << -( b >= -( b && !( b && c ) ) + ( a || !b ) - a - c <= b > +!b || -(-(5%17)) <= +a >= b > a < b ) << endl;















    return 0;
}

