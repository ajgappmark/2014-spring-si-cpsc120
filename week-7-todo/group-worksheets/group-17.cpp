/**
 * group members:
 * week 7
 * worksheet -- group 17
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( c + a < -a * (13%19) >= +( c * +b < b > !a && b && a >= -c && b ) == !(!b) - a * (23/2) || a ) << endl;















    return 0;
}

