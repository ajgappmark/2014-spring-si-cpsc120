/**
 * group members:
 * week 7
 * worksheet -- group 17
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << -( c >= a <= a < a >= !b > a == ( !b < ( b && -a < b ) || c && -b < a <= !b ) <= !b <= !(29%23) ) << endl;















    return 0;
}

