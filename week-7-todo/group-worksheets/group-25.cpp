/**
 * group members:
 * week 7
 * worksheet -- group 25
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( c >= c == b > a >= +a - +( (13%2) > a && a * +b > !c && b || c != a ) && !a - !b < +a ) << endl;















    return 0;
}

