/**
 * group members:
 * week 7
 * worksheet -- group 25
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( a > a || -a || a < !a && +b < (7/3) > b <= b > !a == b < !b != !a < c <= a < (13%3) <= c ) << endl;















    return 0;
}

