/**
 * group members:
 * week 7
 * worksheet -- group 28
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( a > ( b && a < ( (7/7) > a == c || b <= +b != a <= !a <= !a <= b || a < (7/23) ) * c ) ) << endl;















    return 0;
}

