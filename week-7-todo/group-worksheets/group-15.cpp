/**
 * group members:
 * week 7
 * worksheet -- group 15
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( b || a > a - a > b == b <= b > b != (3%7) != (29/13) != a * ( b && !(11%2) ) < c > -(13/11) ) << endl;















    return 0;
}

