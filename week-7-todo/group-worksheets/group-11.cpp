/**
 * group members:
 * week 7
 * worksheet -- group 11
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( c <= c + b * ( a * b < a == a < b || -a ) >= a - a != b * b != (3%13) || b - b ) << endl;















    return 0;
}

