/**
 * group members:
 * week 7
 * worksheet -- group 11
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( (3/3) <= a < a < +a || b && (29%5) != (3%23) == -b >= !c || a * a > -!b >= (23%17) >= -c <= b ) << endl;















    return 0;
}

