/**
 * group members:
 * week 7
 * worksheet -- group 18
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( c < ( (17/5) != c <= !(!b) > (19/17) >= b == !a < a && -c < c == a || a && +!b || b >= -a ) ) << endl;















    return 0;
}

