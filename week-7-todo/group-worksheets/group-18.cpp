/**
 * group members:
 * week 7
 * worksheet -- group 18
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( c == a >= b < c < a < b || b >= !(7/5) >= (19%11) || b != !a <= -b <= a == b < c - a <= !c ) << endl;















    return 0;
}

