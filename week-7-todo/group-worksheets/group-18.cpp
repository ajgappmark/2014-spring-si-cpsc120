/**
 * group members:
 * week 7
 * worksheet -- group 18
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( a <= +( b - a || c != !c && -b ) >= ( a * ( b > -b ) + -a + (2%19) < -a ) <= !b == b ) << endl;















    return 0;
}

