/**
 * group members:
 * week 7
 * worksheet -- group 09
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( ( (5/17) <= -!-b < a <= b >= a > ( a > ( a * b >= !c ) ) >= a < -a ) || a <= b <= a <= a || c ) << endl;















    return 0;
}

