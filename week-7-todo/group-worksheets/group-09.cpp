/**
 * group members:
 * week 7
 * worksheet -- group 09
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( b || (2%3) > -a < b < !( c >= a > c > (11%29) + -a ) && ( b - ( a < b < -b != c < (29%11) ) ) ) << endl;















    return 0;
}

