/**
 * group members:
 * week 7
 * worksheet -- group 01
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( (23/3) || ( !a < b >= +b ) >= a < b < a < +b > +( a <= b >= -b <= c ) < !b >= b <= !c <= b ) << endl;















    return 0;
}

