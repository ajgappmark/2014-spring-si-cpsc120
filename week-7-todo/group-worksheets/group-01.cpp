/**
 * group members:
 * week 7
 * worksheet -- group 01
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( c == a < b - a >= b && !(29/29) >= (29%5) == b == -a >= !b && a >= +c * a > !(23%19) || b ) << endl;















    return 0;
}

