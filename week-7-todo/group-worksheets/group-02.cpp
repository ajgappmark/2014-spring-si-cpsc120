/**
 * group members:
 * week 7
 * worksheet -- group 02
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( (11/29) <= -c >= c > b >= b + b <= -b > a && b < b == !( (23%11) < a ) != !b >= +a && c ) << endl;















    return 0;
}

