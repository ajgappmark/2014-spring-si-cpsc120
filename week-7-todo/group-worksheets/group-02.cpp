/**
 * group members:
 * week 7
 * worksheet -- group 02
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( ( b != ( !a != a ) ) || a || -b <= a != c != +(17%3) * a && (23/17) && b - a && a <= a && -!a ) << endl;















    return 0;
}

