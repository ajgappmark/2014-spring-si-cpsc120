/**
 * group members:
 * week 7
 * worksheet -- group 12
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( b || !b > b && b <= +a == b != !( (19%7) < a && !(7/29) <= -b * (29/17) < a < -c > a ) + a ) << endl;















    return 0;
}

