/**
 * group members:
 * week 7
 * worksheet -- group 24
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( ( a + !b >= -c == (29/23) >= !(23%3) && -b >= !a + !+(7/17) > !a && -b ) >= ( b < a ) ) << endl;















    return 0;
}

