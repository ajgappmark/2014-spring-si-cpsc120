/**
 * group members:
 * week 7
 * worksheet -- group 24
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( c > a || a != -a - -b >= a == -b && !b && !a > a <= a == b && ( a == b >= +b < +(3%13) ) >= b ) << endl;















    return 0;
}

