/**
 * group members:
 * week 7
 * worksheet -- group 24
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << -( a && c != ( b <= b >= b || +a ) && a == !c <= b >= ( b > !a >= (3%13) != a > !b < !a ) ) << endl;















    return 0;
}

