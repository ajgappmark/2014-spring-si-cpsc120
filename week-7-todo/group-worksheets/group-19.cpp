/**
 * group members:
 * week 7
 * worksheet -- group 19
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( (5%2) + !a && !a > +a >= !c < b == (23%17) && c < c > +( a == !+c && b || -(2%23) ) > a * a ) << endl;















    return 0;
}

