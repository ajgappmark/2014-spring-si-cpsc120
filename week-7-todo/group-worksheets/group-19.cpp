/**
 * group members:
 * week 7
 * worksheet -- group 19
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( a > -a + !(5/3) < a - +(13/5) < a || b >= c != a == -b > b >= -c <= a <= -a || b ) << endl;















    return 0;
}

