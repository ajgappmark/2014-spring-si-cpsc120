/**
 * group members:
 * week 7
 * worksheet -- group 08
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( ( b != a <= b > ( c && +b ) != !a >= (29/3) >= a ) <= b < b < b == b < !a + (2%17) == !a ) << endl;















    return 0;
}

