/**
 * group members:
 * week 7
 * worksheet -- group 08
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( !c == -c == a < b * c < !a != -( -(5%5) && ( c && b && b <= !a ) > b ) < ( ( b + -b ) != b ) ) << endl;















    return 0;
}

