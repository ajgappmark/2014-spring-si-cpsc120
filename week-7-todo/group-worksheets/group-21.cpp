/**
 * group members:
 * week 7
 * worksheet -- group 21
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( a < +( (19/7) == c >= a ) <= b < (3%2) <= a - -b < b || !a || a || +(11/17) && c == -c != b ) << endl;















    return 0;
}

