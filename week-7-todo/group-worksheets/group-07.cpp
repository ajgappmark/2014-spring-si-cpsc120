/**
 * group members:
 * week 7
 * worksheet -- group 07
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( b != ( !b || ( c || +a && b - !a || !( a < -(23%19) + b < a ) ) ) * b >= b <= c != c && a ) << endl;















    return 0;
}

