/**
 * group members:
 * week 7
 * worksheet -- group 05
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << !( (19/3) <= a && a < a == a <= b < ( b - b >= !b <= a ) > b || c || c < +!a && b != !(17/17) ) << endl;















    return 0;
}

