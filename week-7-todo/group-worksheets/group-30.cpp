/**
 * group members:
 * week 7
 * worksheet -- group 30
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << -( b == b + c < a < c < a == (5/2) < !b > ( b != -c + a || a && ( c < !a ) && b || -b ) == -c ) << endl;















    return 0;
}

