/**
 * group members:
 * week 7
 * worksheet -- group 30
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( c < a < (2%17) != !a <= (5/11) && a && !b + b >= (19/17) == b != !(2/11) == c - a != a >= c ) << endl;















    return 0;
}

