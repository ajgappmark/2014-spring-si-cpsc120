/**
 * group members:
 * week 7
 * worksheet -- group 04
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( a < ( !c || b <= -a == !( b <= b > -a < (29/13) ) == !a ) - -(-b) > +(29%13) ) << endl;















    return 0;
}

