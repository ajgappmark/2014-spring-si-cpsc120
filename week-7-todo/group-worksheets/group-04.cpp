/**
 * group members:
 * week 7
 * worksheet -- group 04
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( ( a > !a > b <= !b != a ) * ( a - c != a && (19%23) != (11/2) ) < b || b + c && (5%17) && c ) << endl;















    return 0;
}

