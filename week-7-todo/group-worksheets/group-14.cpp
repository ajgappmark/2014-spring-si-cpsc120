/**
 * group members:
 * week 7
 * worksheet -- group 14
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( c || !(!c) >= a != a <= b || ( a || b || a && -a != a && !b < +b == a < -b <= c || !c ) ) << endl;















    return 0;
}

