/**
 * group members:
 * week 7
 * worksheet -- group 27
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( -a <= ( a != !b != c > -( c <= b ) > -a >= a > b ) < c >= b > a >= b - +(7%29) < (17/3) < a ) << endl;















    return 0;
}

