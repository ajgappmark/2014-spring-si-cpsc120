/**
 * group members:
 * week 7
 * worksheet -- group 27
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( a > b > ( b + !a <= !b > !a >= a >= b || b <= c ) > !a < ( b * ( (17%7) <= !( b != b ) ) ) ) << endl;















    return 0;
}

