/**
 * group members:
 * week 7
 * worksheet -- group 29
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( ( b != b >= b && b == b || -a > b == c - !b > b <= !a != a ) && !b || b && c ) << endl;















    return 0;
}

