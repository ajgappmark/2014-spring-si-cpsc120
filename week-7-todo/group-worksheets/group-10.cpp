/**
 * group members:
 * week 7
 * worksheet -- group 10
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( !a != ( ( b < b * (17/11) ) < b ) && b == ( a >= b == b == a >= -(29%7) && b * a || a && c ) ) << endl;















    return 0;
}

