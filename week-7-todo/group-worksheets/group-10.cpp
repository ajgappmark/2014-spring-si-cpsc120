/**
 * group members:
 * week 7
 * worksheet -- group 10
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( a + b - b >= a || a && (11/29) != !b || c >= !c >= a > b != a > !b >= (19%5) - !a != b ) << endl;















    return 0;
}

