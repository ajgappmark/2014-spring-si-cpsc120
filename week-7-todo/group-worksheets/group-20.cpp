/**
 * group members:
 * week 7
 * worksheet -- group 20
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( c && b + b <= b <= a * !c != -a || a > b != ( a >= (19%7) == !(29/23) >= !(23/23) && -a ) >= b ) << endl;















    return 0;
}

