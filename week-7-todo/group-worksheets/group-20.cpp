/**
 * group members:
 * week 7
 * worksheet -- group 20
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << ( a + ( b || +b >= +a == b ) || a > !a * a >= b != ( c > a > a ) >= (7%17) != a * b ) << endl;















    return 0;
}

