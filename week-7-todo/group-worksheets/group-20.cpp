/**
 * group members:
 * week 7
 * worksheet -- group 20
 */

#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main() {
    int  a;
    int  b;
    char c;

    cout << "Enter two integers (separated by a space): ";
    cin >> a >> b;

    cout << "Enter a character: ";
    cin >> c;











    cout << -( a > a > ( b != (23%17) >= b + c <= (19/23) + -(-a) != !c ) + a && (23%29) > b ) << endl;















    return 0;
}

